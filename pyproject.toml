[project]
name = "bisq-market-intelligence"
version = "0.1.0"
description = "Market data and execution analytics for Bisq markets"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["tests"]

